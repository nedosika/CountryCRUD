import { filter } from "./UtilsFilter";
import { sort } from "./UtilsSort";
import { compare } from "./UtilsCompare";
import { throttle } from "./UtilsThrottle";
import { debounce } from "./UtilsDebounce";

const Utils = {
  filter,
  sort,
  compare,
  throttle,
  debounce
};

export default Utils;
